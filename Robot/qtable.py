from Action import Action
from typing import no_type_check

# Since this only used for representing an action for q-tables it should be bundled with action
# This could go in AI.py but it's already cluttered enough.
