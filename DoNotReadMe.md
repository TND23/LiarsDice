Prioritron:

    Create state management

    Decompose AI into subclasses
        -QLearning
        -Action strategies

    Handle game actions more cleanly

    - The current state representation was implemented because I didn't think through how to transform a state representation into a tensor. I should just store a 2d list anyway and transform it after.
